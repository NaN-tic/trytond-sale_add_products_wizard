Sale: Add Products Wizard
=========================

It allows to select sales and execute the wizard, then, select the products to
add and it will create a line for each product (with quantity 0) in sales in
draft state.
